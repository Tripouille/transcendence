<!DOCTYPE html>
<html>
	<head>
		<title>Transcendence</title>
		<meta name="viewport" content="width=device-width,initial-scale=1">
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>

		<script type="text/javascript">
			window.user_id = <%= session[:user_id] %>;
			var initCurrentUserId = <%= session[:user_id].to_json %>;
		</script>
		<%= render 'templates/guilds' %>
		<%= render 'templates/friend' %>
		<%= render 'templates/chatRoom' %>
		<%= render 'templates/selectMode' %>
		<%= render 'templates/matchmaking' %>
		<%= render 'templates/game' %>
		<%= render 'templates/login' %>
		<%= render 'templates/user' %>
		<%= render 'templates/users' %>
		<%= render 'templates/rules' %>

		<%= stylesheet_link_tag 'application', media: 'all' %>
		<%= javascript_pack_tag 'application' %>

	</head>

	<body>
		<%= render 'layouts/menu' %>
		<%= render 'layouts/friendsList' %>
		<%= render 'layouts/chat' %>
		<%= yield %>
	</body>
</html>
