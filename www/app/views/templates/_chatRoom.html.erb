<script type="template" id="chatRoomTemplate">
	<p><%%= name %></p>
	<div>
		<%% _.each(users, function(user) { %>
			<p><%%= user.login %></p>
		<%% }); %>
	</div>
	<ul>
		<%% if (room_type == 'direct_message') { %>
			<li class="hide">Hide room</li>
		<%% } else { %>
			<li class="leave">Leave room</li>
		<%% } %>
	</ul>
</script>