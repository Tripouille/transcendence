<script type=text/javascript>
	
	/* Creates a temp variable to access currentUser's session for backbone */
	/* maybe to be deleted in the future */
	var initCurrentUser = <%== session[:user].to_json %>;

</script>