<main>
</main>
<div>
	<p class="notice"><%= notice %></p>
	<p class="alert"><%= alert %></p>
	<p>Session = <%= @session.inspect %></p>
	<p><%= debug session.to_hash %></p>
	<%= link_to "Sign in with 42", user_marvin_omniauth_authorize_path if !current_user %>
	<%= link_to "Sign out", destroy_user_session_path, method: :delete if current_user %>
</div>
